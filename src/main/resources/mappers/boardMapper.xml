<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.four.mappers.boardMapper">
	<select id="getList" resultType="com.four.myapp.domain.MainDto">
		select topic_tbl.topic_no, user_no, topic_type, topic_title, topic_short_cont, topic_progress, debate_pro, debate_con, debate_neut from topic_tbl left join debate_tbl on topic_tbl.topic_no = debate_tbl.topic_no;
	</select>
	
	<select id="read" parameterType="Integer" resultType="com.four.myapp.domain.MainDto">
		select * from topic_tbl where topic_no = #{topic_no};
	</select>
</mapper>